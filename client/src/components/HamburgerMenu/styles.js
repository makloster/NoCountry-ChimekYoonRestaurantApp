import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  hamburgerMenuContainer: {
    alignItems: "center",
    width: 40,
    height: 40,
    position: "absolute",
    top: 60,
    left: "5%",
  },
});

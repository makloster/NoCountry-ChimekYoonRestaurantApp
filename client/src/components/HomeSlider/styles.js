import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
   backgroundColor: "#22191C",
   height: "100%",
   position: "absolute",
   top: 400
  },
  description: {
    color: "#FFFFFF"
  }
});
